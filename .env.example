# Copy to `.env.local` for local development (never commit secrets).

# Server-only Nest API base (do not expose as NEXT_PUBLIC_*)
HAM_API_BASE_URL=http://localhost:3000/api/v1

# Public web origin (links, CSRF allowlist)
NEXT_PUBLIC_APP_URL=http://localhost:3001
